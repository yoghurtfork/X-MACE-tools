# SHARC - SCRIPTS

Folder contains scripts to run SHARC 
SHARC itself runs from a separate program, but requires careful setup of data points 
The scripts here help to create the input folders required by SHARC

So that we can test out strategies like a dataset that runs from grid points / 
starting from md midway etc 